import { LocationTotalStat } from './location-total-stat';

describe('LocationTotalStat', () => {
  it('should create an instance', () => {
    expect(new LocationTotalStat()).toBeTruthy();
  });
});
